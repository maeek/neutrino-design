/* eslint-disable no-unused-vars */
import React from 'react';
import Avatar, { AvatarProps } from './base';

export const AvatarCircle = (props: AvatarProps) => (
  <Avatar {...props} type='circle' />
);

export default AvatarCircle;
