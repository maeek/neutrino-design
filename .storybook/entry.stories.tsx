import { storiesOf } from "@storybook/react";

const stories = storiesOf("Welcome", module);

stories.add("to Neutrino design", () => (
  <div>This is an atomic design component library built to reuse components across many projects for rapid development.</div>
));
